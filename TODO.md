# TODO - Perbaikan Data Fetch & Settings

- [x] Tambah auto-refresh `useSheetData` di `app/(main)/page.tsx`.
- [x] Tambah auto-refresh `useSheetData` di `app/(main)/gallery/page.tsx`.
- [x] Rapikan `app/admin/settings/page.tsx` (hapus `whatsapp_number` dari `labelMap`).
- [ ] Jalankan lint dan verifikasi hasil.
- [ ] Update TODO setelah semua langkah selesai.
